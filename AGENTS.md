# Rehearsal — transaction rehearsal POC

- Read `.claude/harness-config.json` → `docs/AGENT_BRIEF.md` → `docs/STATUS.md` → `docs/NEXT_PLAN.md` → recent `docs/PROGRESS_LOG.md`.
- Current product authority: `docs/plans/2026-09-12-preflight-impact.md` and `docs/test/preflight-impact.md`. The user reframed Rehearsal as pre-execution impact measurement: request → frozen plan → isolated simulations → impact report → explicit decision. Default AWS UI uses the separate simulation-only preview runtime; no commerce invocation/write permission, no Amazon connector or real payments. Nova initially proposed A; twelve audited simulations recommend B for the tested set. A/B/C outcomes 2/4, 4/4, 0/4; 7 actual Nova calls/USD0.006197, commerce rows 0, runtime independently absent. Current cumulative Nova usage 3626 calls/USD4.380601, unresolved zero; shared cloud budget remaining USD3.838425. Actual Chrome report/decision/reload, English 2:53 Daniel video, AWS draw.io and full local gates are recorded in the audit. No further model run, budget reset, commit/push, YouTube upload or Devpost Submit is active. Prior CW/HTML authority and model-efficacy work below are historical foundations; do not resume automatically.
- Historical design: `agents-for-human-propsal.html`, `docs/plans/2026-09-06-custom-world-poc-plan.md`, `docs/design/CUSTOM_WORLD_FIDELITY.md`. Development setup: `docs/DEVELOPMENT.md`. Current submission: `submissions/devpost.md`.
- Historical baseline before the Nova live run below. The user requested implementation in CW priority order. CW00 contracts and CW01/CW02 world/verifier are implemented. CW03 has raw-provider usage/frozen-policy wiring; CW04 has tool-free independent review, shared review budgeting and known scripted reevaluation verified with SDK fixtures. B3 initial buying, review and candidate buying now share call/cost admission with role-level tokens, failures, unresolved reservations and pre-call rejections verified by SDK fixtures (docs/test/b3-shared-accounting.md); this is not real model or efficacy evidence. Shared total token/tool admission and a frozen 20-cell known pilot (five B0 executions; fifteen B1/B2/B3 NOT_RUN cells) are now verified. B0 no longer spends on an available subset when another required item has no executable visible quote; the changed B0 has been rerun against Medusa with prior 380/360 outcomes preserved and no order/payment for unavailable tents with available lights (docs/test/b0-medusa-rerun.md). English submission drafts are local only (docs/test/shared-limits-and-pilot.md). B2 single-agent simulation retains one dialog across two isolated experiments with shared accounting, verified with SDK fixtures (docs/test/b2-single-agent-simulation.md). B2/B3 frozen-policy evaluation is now wired to a fresh Agent/world with shared learning/evaluation budgets, verified only with SDK fixtures (docs/test/frozen-model-evaluation.md). A frozen known-grid batch (20 conditions × four arms) now has durable global budget admission, full denominators, independent artifact audits and interruption handling verified by SDK fixtures (docs/test/evaluation-batch.md). Real-model and held-out evaluation remain open. Developer observation protocol exists with zero participants. Linux amd64 reproduction passes in a same-host container after a build-only environment fix: Python 663, mypy 57, browser 25, B2/B3/frozen/80-cell SDK checks and Medusa compilation (docs/test/linux-reproduction-20260912.md). Independent-host and Linux Medusa DB/HTTP reproduction remain open. The 2026-09-12 autonomous-completion request authorizes free dependency installation; paid model execution awaits a cost response (docs/plans/2026-09-12-completion.md). CW05 has a metered frozen-policy HTTP model runner with buyer-only config and separate export attestation, verified with SDK fixtures and actual Medusa 310 (docs/test/metered-http-model.md). A persistent session CLI now hands off buyer-only configuration, preserves frozen inputs and exports evidence at shutdown, verified with SDK-fixture/idle sessions against real Medusa (docs/test/model-session.md). Aggregate learning/external costs and event-driven model replanning remain open. CW05 has verified Medusa HTTP, an independent seller, six known frozen B0 practice/Medusa executions, live F03 SSE recovery, F05 supplier-prose/HTTP enforcement, and scripted recovery after actual stock reductions (B 380; A/C 415). CW06 has read-only SSE UI with live Medusa B 380/browser restart evidence; independent export re-verification and snapshot comparison are also verified in the UI. Read-only per-order/supplier-stock observations are verified with live Store GET/SSE/UI. Bounded latency samples and exclusions are verified with live Medusa (24 deliveries, 22 rendered states); this does not establish the overall latency target. Two separate Chromium consumers have verified independent cursor/retention recovery and convergence against the same durable v12 observation. Model replanning remains open. A controlled two-run checkout race against one Medusa process verified overlapping HTTP, one delivery and the losing UNKNOWN reservation; retries/restart did not resubmit checkout. Real model calls, Peer Review efficacy, learned policy transfer, F05 model judgment and distributed/other race schedules remain unverified. Follow docs/STATUS.md and docs/test/frozen-policy-transfer.md. Never infer model efficacy from B0 or scripted SDK fixtures.
- Current live-model authority: docs/test/nova-final-submission.md and docs/plans/2026-09-12-nova-live.md. The user approved global.amazon.nova-2-lite-v1:0 with a total USD10 budget and first USD1 experiment. Real Nova practice and Medusa purchases are independently COMPLETE 310; actual stock changes produced two COMPLETE 380 runs and one deadline failure. A prospective 80-cell batch finished with B0/B1/B2/B3 goals 15/11/8/7 of 20 each. After clarifying accelerated delivery waiting, a separate known pilot finished 4/4/3/4 of five each; preserve the pre-change evidence and source. The final actual Nova English video is 3:45 with local playback/caption verification. Cumulative 3466 calls / USD4.191815, unresolved reservations zero; no paid job is running. A retrospective audit of nine actual Nova supplier-prose cells confirms the known practice scenario preserved canonical 310/recipient/budget, with no out-of-schema tool attempts; three final texts misleadingly mention reserved funds despite ledger reserved=0. This is not general attack resistance or Nova+Medusa F05 evidence (docs/test/untrusted-supplier.md). Peer Review superiority and independent held-out generalization are unproven. All costs remain in .local/nova-live-20260912/campaign.json. Public source/video, entrant details and final submission remain open; no commit/push/publication has occurred. The old Sonnet cost-response blocker is superseded.
- Practice-batch token/monotonic-duration reports are available through `python -m rehearsal.evaluation.metrics <batch-directory>` (docs/test/evaluation-metrics.md). Latest macOS gate: Python 690, mypy 58. Missing/legacy timing remains unavailable; recorded SDK timings are not model-performance evidence. See the Nova live plan for the current approved budget.
- `make commerce-demo` (after `make commerce`) keeps the verified SDK/Medusa view open for local inspection. Separate browser reload/mobile and final export/cleanup are verified (docs/test/demo-inspection.md). This demo buyer remains scripted; human participants remain zero. Separate Nova live calls are recorded in the live plan.
- The HTML's four scenes and generated images are illustrative. No measured product success exists. Earlier Aftercare files are in `archive/aftercare/`; do not resume their P01 tasks or old auto seeds.
- `make check` is the offline gate for documents and installed development foundations. `make check-browser` and `make commerce-smoke` have separate browser/server scope. Missing dependencies must fail. Never infer cloud or transfer success from these checks.
- Use `scripts/dev/with-env.sh` for pinned runtimes. Keep environment installs and secrets in ignored local paths. Local services bind loopback and use only the `rehearsal-dev` Compose project. Preserve other projects, containers and data volumes.
- Preserve unrelated work. Do not edit or import the private sibling `platform-agent`.
- Unattended work consumes only approved `[auto]` items, one at a time. Installation, network/cloud access, publication, database operations and global configuration changes are not authorized by an offline seed. No auto items are currently active.
- Do not commit or push during ordinary interactive work without a user request. An explicitly requested overnight run authorizes that runner's local commits for approved items, never a push.
- Before the first overnight run, establish a reviewed initial commit and clean worktree. Runtime helpers and engineering bibles come from the plugin; do not vendor them. `make overnight-where` prints their source.
